[tool.poetry]
name = "pythonproject(product)"
version = "0.1.0"
description = ""
authors = ["dima <zik871@outlook.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
